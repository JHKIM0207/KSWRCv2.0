# IGRF-14 + T96 자기권 설정

이 저장소는 GitHub Actions에서 3D 자기력선을 계산하고 결과를 GitHub Pages
artifact로 배포합니다. 생성된 스냅샷은 저장소에 커밋하지 않습니다.

## GitHub 최초 설정

1. 저장소의 **Settings > Pages**로 이동합니다.
2. **Source**를 **GitHub Actions**로 설정합니다.
3. **Actions > Build and deploy magnetosphere model**에서 **Run workflow**를
   한 번 실행합니다.
4. 배포 job의 성공 여부를 확인한 다음 Pages 주소를 엽니다.

예약 job은 시간당 네 번 실행됩니다. GitHub 사정에 따라 실행이 지연될 수
있으므로, 화면은 3시간이 지난 스냅샷을 오래된 결과로 판단하고 기존 브라우저
근사 모델로 전환합니다. 이때 화면 왼쪽 위 상태 배지에 폴백임을 표시합니다.

## 모델과 위상 분류

- 내부장: IGRF-14
- 외부장: Tsyganenko 1996 (T96)
- 입력: 최근 15분 태양풍 속도와 밀도, IMF By/Bz의 중앙값, Kyoto Dst,
  계산 시각의 dipole tilt
- 동압: 일반적인 4% 알파입자 보정 포함
- 닫힌 선: 양방향 추적 끝점이 모두 지구로 돌아오는 선
- 열린 선: 한 끝점은 지구로 돌아오고 다른 끝점은 외부 추적 경계에 도달하는 선
- 표시 경계: 열린 선은 Shue et al. (1998) 자기권계면과 만나는 지점에서 절단

T96 스냅샷은 준정적 경험 자기장 구성입니다. 전역 MHD처럼 시간 의존 재연결과
플라스마 유동 자체를 계산하지는 않습니다.

## 데이터 수집 변경

NOAA RTSW의 `active` 값은 운용 중인 우주선 소스를 나타내며 일반적인 관측 품질
플래그가 아닙니다. 기록 갱신기와 브라우저 모두 각 시각에서 유효한 active 행을
우선 사용하되, active 소스에 유효값이 없으면 유효한 inactive 행을 사용합니다.
따라서 소스 전환 중 속도와 밀도 이력이 통째로 비는 현상을 막습니다.
